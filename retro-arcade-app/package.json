{
  "name": "retro-arcade-app",
  "private": true,
  "version": "2.0.4",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "check": "vite build",
    "test": "node --test",
    "audit:prod": "npm audit --omit=dev --audit-level=high",
    "preview": "vite preview",
    "setup-dreamcast": "node scripts/setup-dreamcast.js"
  },
  "devDependencies": {
    "@sveltejs/vite-plugin-svelte": "^6.2.1",
    "svelte": "^5.45.2",
    "vite": "^7.3.1"
  },
  "dependencies": {
    "@fontsource/vt323": "^5.2.7"
  }
}
